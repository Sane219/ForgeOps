# Web-specific overrides. Most env vars live in the root .env.
# Anything beginning with NEXT_PUBLIC_ is bundled into the client.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_APP_NAME=ForgeOps
