export { cn } from './lib/utils';
// Cross-app components land in Phase 4 (Day 6) — shadcn primitives are
// installed into apps/web first via the shadcn CLI, then promoted here when
// a second consumer (storybook, docs site, etc.) appears.
