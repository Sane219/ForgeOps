# Generated by ForgeOps v{{metadata.forgeopsVersion}}
# Template: python-worker
# Service: {{service.name}} (workspace: {{workspace.name}})
# Version: {{version.version}} | Image: {{version.image}}
# Generated at: {{metadata.generatedAt}}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{kebab service.name}}
  namespace: argocd
  labels:
    app.kubernetes.io/name: {{kebab service.name}}
    app.kubernetes.io/part-of: {{kebab workspace.name}}
    managed-by: forgeops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/{{kebab workspace.name}}/{{kebab service.name}}.git
    targetRevision: main
    path: helm
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: {{kebab workspace.name}}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
